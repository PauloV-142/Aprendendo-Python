### Board Documentation

#### Initialization:
`size = (width, height)`

---
#### Properties:
`w = width`
`h = height`


`points = { }`
###### The set of points (x, y) plotted on the board.

---
#### Methods:
*Unsure if I'll use tuples or simpler parameters for functions.*


`plot((x, y), icon)`
###### Append to the points set.

`unplot(())`
###### Remove from the points set

`update()`
######